name: Trim builds
description: Trims old builds when the disk space fills up on the machine
author: https://github.com/samsarahq
requirements: []
configuration:
  properties:
    minimum:
      type: number